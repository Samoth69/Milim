apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: network-policy
spec:
  endpointSelector:
    matchLabels:
      - app: traefik-internal
  ingress:
    # nothing allowed
    - {}
  egress:
    # nothing allowed
    - {}
